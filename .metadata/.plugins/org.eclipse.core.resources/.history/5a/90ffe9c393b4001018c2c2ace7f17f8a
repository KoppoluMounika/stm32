#include "stm32l4xx.h"
#include <stdio.h>
#include "uart.h"

int main(void)
{
    USART2_tx_rx_init();

    while (1)
    {
        printf("Hello STM32 UART\r\n");
        for (volatile int i = 0; i < 1000000; i++);
    }
}
